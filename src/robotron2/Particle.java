package robotron2;

public class Particle {
	
/*	
	shotgun goes off, airburst after 3 seconds, ricochet last
	
	set to True immediately
	based on time, can be set to true immediately
	at the end of the airburst code, have a boolean set to true that then activates the next section of code
	
	
	
	
	
	
	
	for(guhprime) {
		if(guh) {
			airburst;
			code;
			airburst timer = 10;
			boolean = true;
		}
		if(boolean = true && bullet.airbursttimer == 0) {
			shotgun = true;
			boolean2 = true;
		}
		if(boolean2 = true && bullet.airbursttimer == 0) {
			ricochet = true
		}
	}
*/
	
}